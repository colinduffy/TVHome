<UserControl x:Class="TVHome.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right" FontSize="30" Name="timeText"/>
        <TextBlock Grid.Column="0" VerticalAlignment="Bottom" FontSize="35" Name="weatherText"/>
        <TextBlock Grid.Column="0" VerticalAlignment="Bottom" FontSize="35" Name="temperatureText"/>
    </Grid>
</UserControl>
